<p>
  <strong><%= comment.user.name %>:</strong> <%= comment.body %>
</p>
<p>
  <% if @tweet.user.id == @current_user.id || comment.user_id == @current_user.id %>
    <%= link_to 'Delete Comment', [comment.tweet, comment],
                 method: :delete,
                 data: { confirm: 'Are you sure?' } %>
  <% end  %>
</p>
