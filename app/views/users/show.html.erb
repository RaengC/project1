
    <div class="col-md-4">
      <section>
        <h1>
          <%= image_tag @user %>
          <%= @user.name %>
        </h1>
      </section>

      <section>
        <%= render 'users/stats' %>
      </section>
    </div>

    <div class="col-md-8">
      <section>
        <%= render 'users/follow_form' if @current_user.present?  %>
      </section>

    </div>


    <section>
      <%= render 'tweets' %>
    </section>
