<h2>Twitter Clone<h2>
<% if @current_user.present? %>
  <p>You are currently logged in as <%= @current_user.name %></p>

  <div class="home-page">
    <%= render 'tweets/tweets' %>
  </div>

<% end %>
